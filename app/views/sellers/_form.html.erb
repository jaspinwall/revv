<%= form_for(seller) do |f| %>
  <% if seller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seller.errors.count, "error") %> prohibited this seller from being saved:</h2>

      <ul>
      <% seller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :publishable_key %>
    <%= f.text_field :publishable_key %>
  </div>

  <div class="field">
    <%= f.label :secret_key %>
    <%= f.text_field :secret_key %>
  </div>

  <div class="field">
    <%= f.label :stripe_user_id %>
    <%= f.text_field :stripe_user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
